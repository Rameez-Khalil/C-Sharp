/*
 * Method Overloading: 
 *      An ability of a function to behave differently based off of the parameters provided.
 *      This happens in the same class, so if we have two methods named show, then those methods should be differentiated based off of their parameters.
 */